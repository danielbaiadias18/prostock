import { ExcluirDirective } from './excluir.directive';

describe('ExcluirDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcluirDirective();
    expect(directive).toBeTruthy();
  });
});
