import { AlterarDirective } from './alterar.directive';

describe('AlterarDirective', () => {
  it('should create an instance', () => {
    const directive = new AlterarDirective();
    expect(directive).toBeTruthy();
  });
});
