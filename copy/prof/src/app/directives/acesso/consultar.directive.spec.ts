import { ConsultarDirective } from './consultar.directive';

describe('ConsultarDirective', () => {
  it('should create an instance', () => {
    const directive = new ConsultarDirective();
    expect(directive).toBeTruthy();
  });
});
